# Netlify 部署指南

## 🚀 Netlify 优势

✅ **完全免费** - 个人项目免费使用  
✅ **简单易用** - 拖拽部署，无需复杂配置  
✅ **持续部署** - 连接Git仓库，自动部署  
✅ **全球CDN** - 全球加速，访问速度快  
✅ **HTTPS自动** - 自动SSL证书  
✅ **功能丰富** - 表单处理、身份验证等  

## 📋 部署步骤

### 方法一：拖拽部署（最简单）

1. **构建项目**
   ```bash
   npm run build
   ```

2. **访问Netlify**
   - 打开 [netlify.com](https://netlify.com)
   - 注册/登录账号

3. **拖拽部署**
   - 将 `dist` 文件夹拖拽到Netlify部署区域
   - 等待部署完成

### 方法二：Git连接部署（推荐）

1. **创建Git仓库**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Vue3 服务电话指南"
   
   # 推送到GitHub/GitLab
   git remote add origin https://github.com/YOUR_USERNAME/lifehelpservice.git
   git branch -M main
   git push -u origin main
   ```

2. **连接Netlify**
   - 在Netlify点击 "New site from Git"
   - 选择您的Git提供商（GitHub/GitLab）
   - 选择 `lifehelpservice` 仓库

3. **配置构建设置**
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
   - 点击 "Deploy site"

## 🌐 访问地址

部署成功后，您的网站将获得一个Netlify子域名：

```
https://your-site-name.netlify.app
```

**示例**: 
```
https://lifehelpservice-123.netlify.app
```

## 🔧 自定义域名配置

### 步骤1：添加自定义域名

1. 进入Netlify站点设置
2. 选择 "Domain management"
3. 点击 "Add custom domain"
4. 输入您的域名

### 步骤2：配置DNS

**推荐方法：使用Netlify DNS**

1. 在域名注册商处修改DNS服务器为：
   ```
   dns1.p01.nsone.net
   dns2.p01.nsone.net
   dns3.p01.nsone.net
   dns4.p01.nsone.net
   ```

**备用方法：添加CNAME记录**

```
类型: CNAME
名称: www (或 @)
值: your-site.netlify.app
TTL: 3600
```

## ⚙️ 高级配置

### 环境变量（可选）

在Netlify的 "Site settings" → "Environment variables" 中添加：

```
NODE_ENV = production
VITE_APP_TITLE = "中国常用服务电话指南"
```

### 表单处理（未来扩展）

Netlify支持自动表单处理，未来可以添加反馈功能。

## 📊 部署检查清单

- [ ] 项目构建成功 (`npm run build`)
- [ ] Netlify账号已注册
- [ ] 站点已创建
- [ ] 自定义域名已配置（如需要）
- [ ] HTTPS已启用
- [ ] 网站功能测试通过

## 🔄 更新网站

### Git连接方式（自动）
每次推送代码到 `main` 分支，Netlify自动重新部署。

### 手动更新
```bash
npm run build
# 重新拖拽 dist 文件夹到Netlify
```

## 🛠️ 故障排除

### 常见问题

1. **构建失败**
   - 检查 `package.json` 中的构建脚本
   - 查看Netlify构建日志

2. **样式丢失**
   - 确保CSS文件路径正确
   - 检查 `vite.config.js` 配置

3. **路由404**
   - 确认 `netlify.toml` 中的重定向规则

### 查看日志

在Netlify控制台的 "Deploys" 标签页查看详细构建日志。

## 💡 最佳实践

1. **启用自动部署** - 连接Git仓库实现CI/CD
2. **使用环境变量** - 管理不同环境的配置
3. **定期备份** - 重要数据定期备份
4. **性能监控** - 使用Netlify Analytics监控性能

---

**Netlify部署完成！您的网站现在可以通过专业平台提供服务了！** 🎉